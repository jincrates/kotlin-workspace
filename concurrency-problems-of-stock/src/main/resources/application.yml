spring:
    jpa:
        hibernate:
            ddl-auto: create
        show_sql: true

    datasource:
        driver-class-name: com.mysql.cj.jdbc.Driver
        url: jdbc:mysql://localhost:3306/stock-concurrency
        username: bootuser
        password: bootuser

logging:
    level:
        org:
            hibernate:
                SQL: DEBUG
                type:
                    descriptor:
                        sql:
                            BasicBinder: TRACE